import { vw, vh } from '../utils/dimensions';

export const spacing = {
    // Horizontal spacing — paddingHorizontal, marginLeft/Right, columnGap
    xs: vw(4),
    sm: vw(8),
    md: vw(16),
    lg: vw(24),
    xl: vw(32),

    // Vertical spacing — paddingVertical, marginTop/Bottom, rowGap
    vXs: vh(4),
    vSm: vh(8),
    vMd: vh(16),
    vLg: vh(24),
    vXl: vh(32),
};